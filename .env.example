# JWT Configuration
JWT_SECRET_KEY=your_super_secret_jwt_key_here

# Database Configuration (Optional override, defaults provided for Docker Compose)
DB_HOST=postgres
DB_PORT=5432
DB_USER=myuser
DB_PASSWORD=mypassword
DB_NAME=mydatabase
DB_SSLMODE=disable
